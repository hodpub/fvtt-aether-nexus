{{! Biography Tab }}
<section class='tab stratagem {{tab.cssClass}}' data-group='primary' data-tab='stratagem'>
  <label>{{localize "AETHER_NEXUS.Actor.Nemeses.FIELDS.stratagem.label"}}</label>
  {{! Editors must receive enriched text data from getData to properly handle rolls, UUID links, etc. }}
  {{#if editable}}
  <prose-mirror name="system.stratagem" data-document-u-u-i-d="{{actor.uuid}}" value="{{system.stratagem}}" collaborate="true"
    toggled="true">
    {{{enrichedStratagem}}}
  </prose-mirror>
  {{else}} {{{enrichedStratagem}}}
  {{/if}}
</section>