{{log this}}

<section class='tab quality-list foe-list {{tab.cssClass}}' data-group='primary' data-tab='quality-list'>
  {{#each item.qualities as |item key| }}
  <div class="quality-item" data-item-id="{{item.id}}">
    <label type='text'>{{item.name}}</label>
    {{{item.system.description}}}
  </div>
  {{/each}}
</section>